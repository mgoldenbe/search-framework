#ifndef SLB_EXTENSIONS_HEURISTICS_HEADERS_H
#define SLB_EXTENSIONS_HEURISTICS_HEADERS_H

/// \file
/// \brief The heuristics (not heuristic policies, so they do not accept the
/// algorithm type as a template parameter).
/// \author Meir Goldenberg

namespace slb {
namespace ext {

/// \namespace slb::ext::heuristic
/// The heuristics (not heuristic policies, so they do not accept the
/// algorithm type as a template parameter).
namespace heuristic {}

} // namespace
} // namespace

#include "differential.h"

#endif
